{
  "name": "fool-journey",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Статическая браузерная игра «Путь Шута» для изучения Таро.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "check:encoding": "node scripts/check-encoding.mjs",
    "check": "npm run check:encoding && tsc --noEmit",
    "test": "vitest run",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.3",
    "vite": "^7.1.12",
    "vitest": "^3.2.4"
  }
}
